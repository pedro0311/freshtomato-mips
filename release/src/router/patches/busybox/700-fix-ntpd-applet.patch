--- busybox/networking/ntpd.c
+++ busybox/networking/ntpd.c
@@ -585,8 +585,8 @@
 {
 	uint32_t intl;
 	uint32_t frac;
-	intl = (uint32_t)(time_t)d;
-	frac = (uint32_t)((d - (time_t)d) * 0xffffffff);
+	intl = (uint32_t)d;
+	frac = (uint32_t)((d - (int64_t)d) * 0xffffffff);
 	lfp->int_partl = htonl(intl);
 	lfp->fractionl = htonl(frac);
 }
